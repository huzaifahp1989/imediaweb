# Copy this file to `.env` (for local dev) and set real values.
# Vite loads `.env` / `.env.local` at startup. Restart the dev server after changes.

# Firebase project configuration (from Firebase Console → Project Settings → Your apps)
VITE_FIREBASE_API_KEY=YOUR_API_KEY
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_SENDER_ID
VITE_FIREBASE_APP_ID=YOUR_APP_ID

# Use Netlify backend for messages (optional; enable on Netlify)
# Set to true to route message operations through serverless functions
VITE_USE_BACKEND=false

# Admin gating (optional but recommended)
# Exact email used to sign in via Firebase; required for AdminGuard to allow access
VITE_ADMIN_EMAIL=admin@example.com

# Simple 2FA gate for AdminLogin. For production, enable Firebase Multi-factor instead.
VITE_ADMIN_2FA_CODE=123456

# Setup notes
# 1) In Firebase Console → Build → Authentication → Sign-in method: enable Email/Password.
# 2) In Firebase Console → Build → Firestore Database: create a database (Start in production).
# 3) Firestore collection `messages` will be created on first Contact form submission.
# 4) Optional: Firestore Security Rules (restrict read/write to admin email):
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /messages/{docId} {
#          allow read, write: if request.auth != null && request.auth.token.email == "admin@example.com";
#        }
#      }
#    }
#    Replace admin@example.com with your actual admin email and publish rules.

# Netlify Functions (server-side) configuration
# These are server-side env vars to set on Netlify (not exposed in client)
# Use a Firebase service account with Firestore access
# In Netlify UI → Site settings → Environment variables
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=service-account@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n
# Restrict functions to your admin email
# ADMIN_EMAIL=admin@example.com
